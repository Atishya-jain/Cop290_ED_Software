#include <bits/stdc++.h>
using namespace std;
struct plane {
	float A;
	float B;
	float C;	
};

int main(){


plane tmp;

// vector<vector<plane> > graph;
// vector<plane> a;
// a.push_back(tmp);
// graph.push_back(a);


vector<list<plane> > graph;
list<plane> a;
a.push_back(tmp);
graph.push_back(a);

// cout<<graph<<endl;;
return 0;
}